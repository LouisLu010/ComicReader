# Comic Import Context

ComicReader 的导入上下文负责把用户选择的文件夹解释为漫画结构，并在复制任何内容前生成可审查的导入清单。

## Language

**漫画根目录（Comic Root）**：
用户一次选择并被视为一本漫画的顶层文件夹。
_Avoid_：书库目录、扫描根

**分组（Collection）**：
用于组织下级分组或话的目录，通常对应“卷”，但不限定具体层级或命名。
_Avoid_：卷模型、父章节

**话（Chapter）**：
来自同一目录、具有确定页序的一组页面候选。
_Avoid_：文件夹、相册

**散页话（Loose-pages Chapter）**：
当漫画根目录或分组目录同时含有页面和子级目录时，由这些直属页面形成的话；模型使用 `rootLoosePages` 与 `collectionLoosePages` 区分来源位置。
_Avoid_：散图、孤立页面

**页面候选（Page Candidate）**：
内容被识别为受支持图片的普通文件；候选可以可读，也可以因损坏而不可读。
_Avoid_：图片文件、资源

**轻量内容指纹（Lightweight Content Fingerprint）**：
由文件大小、头部与尾部样本生成的扫描期 SHA-256，仅用于变化筛选和疑似重复提示，不等同于复制阶段的完整文件校验和。
_Avoid_：完整哈希、文件身份

**疑似重复（Suspected Duplicate）**：
同一话内两个或更多可读页面具有相同轻量内容指纹；仅生成提示，不删除或合并页面。M1.1 不比较封面与正文或跨话页面。
_Avoid_：重复页、自动去重

**独立封面资源（Standalone Cover Asset）**：
从根目录散页话中排除、但仍保留在导入清单和空间估算中的封面候选。按当前明确规则，即使根目录只有这一张可读图片也保持排除，并报告没有可读话。
_Avoid_：封面话、第一页

**导入清单（Import Manifest）**：
复制前对一本漫画识别结果的不可变描述，包含层级、页序、封面建议和问题。
_Avoid_：扫描结果、导入任务

**导入问题（Import Issue）**：
导入清单中需要向用户解释的忽略项、损坏项或潜在冲突。
_Avoid_：异常、日志

**源相对路径（Source-relative Path）**：
相对于漫画根目录的稳定路径，用于预览、更新匹配和问题定位，不包含用户设备上的绝对路径。
_Avoid_：文件 URL、内部路径
